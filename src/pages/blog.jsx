import Button from "../component/button"
const blog = ({name}) => {
  console.log(blog);
  
  return (
    <div>
        <Button name={name} />
        <h1>blog</h1>

    </div>
  )
}

export default blog